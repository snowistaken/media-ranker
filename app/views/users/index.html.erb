<body>
  <h2>Index of Users</h2>

  <div>
    <ul>
      <% @users.each do |user| %>
          <li>
          <%= user.username %>
            <%# <%= link_to user.username, user_path(album) %>
          </li>
        <% end %>
    </ul>
  </div>
</body>